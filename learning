13-02-26
today i learn the diffence between models and functions : functions hold input and through that we get parameters wheras models are something that hold parameters 
that can variable in nature means they can be vary accroding to user input and they give output on that basis 
